{
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "customizations": {
        "vscode": {
          "settings": {},
          "extensions": [
            "ms-python.python",
            "hbenl.vscode-test-explorer",
            "ms-toolsai.jupyter",
            "github.copilot"
        ]
        }
    },
    "postStartCommand": "echo 'export PYTHONPATH=/workspaces/Kaggle-tools:$PYTHONPATH'"
}