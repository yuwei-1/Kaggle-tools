# Definitions for the kaggle tools repository.
KTOOLS_DATASET_NAME = "ktools-latest"

# Kaggle specific
INPUT_DIR = "/kaggle/input"
COMPETITION_DIR = f"{INPUT_DIR}/competitions"
DATASET_DIR = f"{INPUT_DIR}/datasets"
OUTPUT_DIR = "/kaggle/working"

# user specific
USERNAME = "noodl35"

# Timeout settings
KAGGLE_TIMEOUT = 42000

# Optuna specific default hyperparameters
OPTUNA_NUM_TRIALS = 500
OPTUNA_TIMEOUT_SECONDS = 39600
